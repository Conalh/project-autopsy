# Sample Reports

These reports are committed regression artifacts generated from stable fixtures.

Update them after intentional report format or detector changes:

```powershell
npm run samples:update
```

Check for unexpected drift:

```powershell
npm run samples:check
```
